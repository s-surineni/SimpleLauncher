<?xml version="1.0" encoding="utf-8"?>
<data-extraction-rules>
    <cloud-backup>
        <!-- Exclude shared preferences from cloud backup -->
        <exclude domain="sharedpref" path="."/>
    </cloud-backup>
    <device-transfer>
        <!-- Exclude shared preferences from device transfer -->
        <exclude domain="sharedpref" path="."/>
    </device-transfer>
</data-extraction-rules>
